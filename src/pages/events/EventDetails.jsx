import PageStub from "../../components/PageStub.jsx";

export default function EventDetails() {
  return (
    <PageStub
      eyebrow="Events"
      title="Event Details"
      description="Featured upcoming events with full descriptions, locations, registration links, and accessibility info."
    />
  );
}
